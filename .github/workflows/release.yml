name: release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build image
        run: docker build -t ghcr.io/${{ github.repository }}/bertscope:${{ github.ref_name }} .
      - name: Push image
        run: docker push ghcr.io/${{ github.repository }}/bertscope:${{ github.ref_name }}
